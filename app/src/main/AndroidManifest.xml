<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniCare"
        android:usesCleartextTraffic="true">

        <!-- MainActivity làm màn hình khởi động để xử lý logic check Login/DevMode -->
        <activity
            android:name=".feature.auth.ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity android:name=".feature.auth.ui.RegisterActivity" />
        <activity android:name=".feature.auth.ui.ForgotPasswordActivity" />
        <activity android:name=".feature.auth.ui.CreateFileActivity" />
        <activity android:name=".feature.patients.doctor.ui.DoctorListActivity" />
        <activity android:name=".feature.patients.doctor.ui.DoctorDetailActivity" />
        <activity android:name=".feature.patients.appointment.viewmodel.BookAppointmentActivity" />
        <activity android:name=".feature.patients.appointment.ui.ConfirmAppointmentActivity" />
        <activity android:name=".feature.patients.appointment.viewmodel.BookVaccineActivity" />
        <activity android:name=".feature.patients.profile.ui.FilePerson" />
        <activity android:name=".feature.patients.profile.ui.EditProfilePerson" />

    </application>

</manifest>