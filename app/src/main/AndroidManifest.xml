<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doctor">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doctor">

        <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity android:name=".DoctorListActivity"   android:screenOrientation="portrait"/>
        <activity android:name=".DoctorDetailActivity" android:screenOrientation="portrait"/>
        <activity android:name=".AppointmentActivity"  android:screenOrientation="portrait"/>
        <activity android:name=".FilterActivity"       android:screenOrientation="portrait"/>

    </application>

</manifest>